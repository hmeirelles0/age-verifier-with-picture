# age-verifier-with-picture

This project was meant as test of my knowledge regarding the use of JavaScript and the following codes:

var x = window.document.createElement()
  //with 'x' being any given variable
.setAttribute('','')
.appendChild()
